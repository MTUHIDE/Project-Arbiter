Errors so far accumulated:

1. Blocks containing other blocks read in <l> tags of those blocks.
2. The scripts handler seems to be outputting the contents of 2 different script tag pairs given only one script to read.
3. Blocks whose parameters are other blocks are not being read in yet.

