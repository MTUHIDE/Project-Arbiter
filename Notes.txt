Errors so far accumulated:

1. Blocks containing other blocks read in <l> tags of those blocks.
2. Blocks whose parameters are other blocks are not being read in yet.

To do:
1. Fix the ordering of "blocks", especially blocks whose parameters are other blocks

12/4/2018:

Began fixing getElementsByTagName returning children of children
